/*#ifndef SPIFFS_UTILS_H
#define SPIFFS_UTILS_H

#include <FS.h>
#include <SPIFFS.h>
#include <Arduino.h>
#include "tinf.h"
*/

/*
class SPIFFSUtils {
public:
    // Función que busca y descomprime un archivo GZIP almacenado en SPIFFS
    static const char* find_string(const char *filename);
};

#endif // SPIFFS_UTILS_H
*/